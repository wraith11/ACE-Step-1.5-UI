// Tailwind CSS v4 uses the `@theme` block in index.css — a separate
// tailwind.config.js is not needed and is intentionally empty/neutral.
// Theme tokens (suno colors, dark mode) are defined in webui/index.css.
export default {};