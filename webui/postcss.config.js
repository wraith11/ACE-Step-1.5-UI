// Tailwind CSS v4 is compiled by the @tailwindcss/vite plugin (see
// vite.config.ts). No PostCSS plugins are needed — this file is intentionally
// empty. Keeping it neutral avoids the legacy v3 postcss+autoprefixer setup.
export default {
  plugins: {},
};